{/* Colors.mdx */}

import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import themeConfig from '../../tailwind.config.js';

<Meta title="Colors" />

<ColorPalette>
   {
      Object.entries(themeConfig?.theme?.extend?.colors).map(([k,v]) => 
         {
            return <ColorItem title={k} colors={ { [k] : v }} />
         }
      )
   }
</ColorPalette>


